ajax
	基本认识
		浏览器与服务器进行数据通信的技术
	axios库
		1.引入axios.js:
		2.使用axios函数：
			axios（{url:'服务器地址'}）.then((result)=>{//后续出路}）
			url:(资源定位符，网址)
				协议
					数据类型
				域名
					服务器
				资源路径
					具体位置
				url查询参数
					axios（{url:'服务器地址'} params:{参数名：值}）.then((result)=>{//后续出路}）
					浏览器提供额外的查询参数，服务器根据查询参数返回对应的数据
						子主题 1
					补充一个小知识点：将字符串映射成为一个li:数组映射：list.map（）
join()转化为数组
						子主题 1
			常用请求方法
				get
					获取数据（默认）
				post·
					提交数据
				put
					修改数据
				delete
					删除
				patch
					修改部分
			调用成功就是then后面的，失败.catch(error => {
)
		判断错误
			请求报文
				浏览器按照http，发送给服务器的内容
					
				错误排查
			响应报文
				
				
			http协议：浏览器发送以及浏览器返回内容的格式
		接口文档
			包括url，请求方法，以及参数
		快速收集表单元素
		bootstrap弹框
	分支主题 3
	分支主题 4
